# ─────────────────────────────────────────────────────────────────
#  Copy this file to .env and fill in your values
#  Never commit .env to git!
# ─────────────────────────────────────────────────────────────────

# From Supabase → Project Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-public-key

# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=["http://localhost:5500","https://yourportfolio.com"]

# JWT — generate a strong random string, e.g.:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=replace-with-a-long-random-secret-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# Admin login — your username (pick anything)
ADMIN_USERNAME=admin

# Admin password hash — generate it by:
#   1. Start the server
#   2. Visit http://localhost:8000/api/auth/hashpw?password=YourPassword
#   3. Copy the "hash" value here
#   4. ⚠️ Delete or disable that /hashpw endpoint in production!
ADMIN_PASSWORD_HASH=replace-with-bcrypt-hash-from-hashpw-endpoint
